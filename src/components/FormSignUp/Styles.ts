import styled from 'styled-components';

export const FormWrapper = styled.section`
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: 3rem;

	h3 {
		font-size: 1.75rem;
	}

	form {
		display: flex;
		flex-direction: column;
		gap: 3rem;
		max-width: 800px;
		width: 100%;
	}
`;
