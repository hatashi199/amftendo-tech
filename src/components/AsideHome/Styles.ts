import styled from 'styled-components';

export const AsideHomeContainer = styled.aside`
	background-color: rgba(0, 0, 0, 0.65);
	display: flex;
	flex-direction: column;
	gap: 0.3rem;
	padding: 3.5rem;

	.white-box {
		background-color: var(--color-white);
		height: 250px;
	}
`;
